"""Streamlit quote UI — Phase 4 (not yet implemented).

Quote form (altitude, area, duration, value) -> premium breakdown card +
three charts: debris density vs altitude, loss-distribution histogram,
premium vs altitude. Target: a full quote in under 3 seconds.

Run with:  streamlit run src/app/streamlit_app.py
"""
